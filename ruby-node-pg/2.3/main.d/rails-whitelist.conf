# nginx strips env vars that are not white listed.
# Standard rails stuff is captured elsewhere in the stack,
# each app needs to whitelist custom env vars as shown below.
env DATABASE_URL;
env REDIS_URL;
